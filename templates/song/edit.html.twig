{% extends "base.html.twig" %}

{% block title %}- Moidifier : <em>{{song.title}}</em> {% endblock %}

{% block h1 %}Moidifier : <em>{{ song.title}}</em> {% endblock %}

{% block main %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 mx-auto">
                {{ form_start(form) }}
                    <div class="mb-3">
                        <label for="">Titre</label>
                        <div class="text-danger">{{ form_errors(form.title) }}</div>
                        {{ form_widget(form.title, {'attr' : {'class' : 'form-control'}}) }}
                    </div>

                    <div class="mb-3">
                        <label for="">Le nom du ou des artistes</label>
                        <div class="text-danger">{{ form_errors(form.artists) }}</div>
                        {{ form_widget(form.artists, {'attr' : {'class' : 'form-control'}}) }}
                    </div>

                    <div class="mb-3">
                        <label for="">La note / 5</label>
                        <div class="text-danger">{{ form_errors(form.score) }}</div>
                        {{ form_widget(form.score, {'attr' : {'class' : 'form-control'}}) }}
                    </div>

                    <div class="mb-3">
                        <input type="submit" class="btn btn-dark" formnovalidate value="Modifier" />
                    </div>
                {{ form_end(form) }}
        </div>
            </div>
        </div>
      
{% endblock %}